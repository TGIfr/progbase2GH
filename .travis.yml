sudo: required
language: c
os:
  - linux
compiler:
  - gcc

script:
  - gcc -std=c99  csv.c lib.a -coverage -lcheck
  - ./a.out
  - gcov -c  csv.c list.c tests.c


after_success:
  - bash <(curl -s https://codecov.io/bash)
  - echo program compiled successfully