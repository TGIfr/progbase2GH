sudo: required
language: c
os:
  - linux
compiler:
  - gcc

script:
  - gcc test.c csv.c list.c list.h csv.h -coverage
  - ./a.out
  - gcov -o test.c csv.c list.c list.h csv.h
  - echo program compiled successfully

after_success:
  - bash <(curl -s https://codecov.io/bash)