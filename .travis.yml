sudo: required
language: c
os:
  - linux
compiler:
  - gcc

script:
  - gcc -std=c99  csv.c list.c list.h csv.h -coverage
  - ./a.out
  - gcov -c  csv.c list.c tests.c
  - echo program compiled successfully

after_success:
  - bash <(curl -s https://codecov.io/bash)